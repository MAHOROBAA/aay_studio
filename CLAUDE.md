# AAY Studio 개발 행동 지침

이 문서는 AAY Studio 저장소에서 작업하는 모든 Claude Code 세션이 항상 따라야 하는 규칙이다.
화면 기획과 상세 정책은 [`docs/spec/aay-studio-spec.md`](docs/spec/aay-studio-spec.md)를 참고하고,
시각적 기준은 Figma 파일 `AAY` (`https://www.figma.com/design/s1vcVinETNj4YPpZH4z35B/AAY`)를 참고한다.

**[`docs/spec/spec-addendum.md`](docs/spec/spec-addendum.md)는 `aay-studio-spec.md` 작성 이후 추가·변경된 내용이며,
충돌 시 이 문서가 우선한다.** 라이브러리(캐릭터·세계관·스토리), 프로젝트/채널 용어, 템플릿 Step 2의 캐릭터 생성 등을
작업할 때는 반드시 먼저 확인한다.

**[`docs/spec/spec-addendum-credit.md`](docs/spec/spec-addendum-credit.md)는 크레딧·결제·GA4 분석 정책 추가 명세다.**
마이페이지 크레딧 UI, 크레딧 충전/결제 화면, GA4 이벤트 연동을 작업할 때 먼저 확인한다. 이 문서 24장에 명시된
"현재 프론트엔드 단계" 범위만 구현하고, 결제 승인/크레딧 차감 등 서버 로직은 임의로 완성하지 않는다.

**"2단계 배치 진행해줘" 같은 요청을 받으면 먼저 [`docs/spec/phase2-batches.md`](docs/spec/phase2-batches.md)를 읽고
어떤 배치가 완료됐고 다음에 뭘 해야 하는지 확인한다.** 이 문서에 없는 새로운 진행 방식을 임의로 정하지 않는다.

## 0. 문서 우선순위

- 화면 레이아웃, 색상, 폰트, 여백 등 시각적 수치는 **Figma를 우선**한다.
- 기능 조건, 예외 정책, 데이터 구조는 **`docs/spec/aay-studio-spec.md`를 우선**한다.
- Figma와 spec 문서가 서로 다른 내용을 말하거나, spec에 없는 화면·정책을 추측해야 하는 상황이면
  임의로 결정하지 말고 사용자에게 먼저 질문한다.

## 1. 기본 원칙

- 현재 저장소와 기존 코드를 먼저 확인한 후 작업한다.
- 요청받은 범위만 수정하며, 임의로 기능·화면·라이브러리를 추가하지 않는다.
- 사용자가 수정한 코드를 임의로 되돌리거나 덮어쓰지 않는다.
- 요구사항이 불명확하거나 선택에 따라 구조가 크게 달라지는 경우 먼저 질문한다.
- 한 번에 전체 서비스를 구현하지 않고, 요청받은 단계 단위로 작업한다 (`docs/spec/aay-studio-spec.md` 11장 구현 우선순위 참고).
- 추후 구현 예정인 기능을 임의로 완성하거나 가짜 동작으로 연결하지 않는다.
- 임시 처리나 추정으로 구현한 부분은 완료된 기능처럼 보고하지 않는다.

## 2. 디자인 구현 원칙

- Figma를 화면과 디자인 규격의 기준으로 사용한다.
- Figma에 없는 디자인을 임의로 추가하거나 변경하지 않는다.
- 기존 색상, 폰트, 여백, 라운드, 테두리 규격을 우선 재사용한다.
- 버튼, 인풋, 드롭다운, 팝업 등 반복 UI는 공통 컴포넌트로 구현한다.
- 같은 기능과 상태를 가진 UI를 페이지마다 별도로 만들지 않는다.
- 버튼 기본 규격: 높이 44px, 글자 14px, 상하 패딩 10px, 좌우 패딩 20px, radius 10px.
- 입력창과 드롭다운의 높이는 38px이다.
- 위험 동작에는 기존 경고색 `#D00000`을 사용한다.
- 팝업 딤은 `#000000` 50%를 사용한다.
- 데스크톱 기본 프레임은 1280 × 832, 공통 헤더 높이 70px, LNB 폭 180px이다.
- 기본 폰트는 `Noto Sans KR`을 우선 적용한다.
- 디자인 값이 불명확하면 기존 화면에서 가장 많이 사용된 규격을 확인하고, 그래도 판단할 수 없으면 질문한다.
- 반응형 처리를 고려하되 데스크톱 피그마 화면을 임의로 재설계하지 않는다.

## 3. 코드 작성 원칙

- React와 TypeScript를 사용한다.
- 함수형 컴포넌트와 React Hooks를 사용한다.
- 의미 없는 `any` 사용을 피하고 필요한 타입을 명시한다.
- 컴포넌트의 역할을 작게 유지하고 페이지에 모든 로직을 몰아넣지 않는다.
- 공통 UI, 페이지 전용 UI, 비즈니스 로직을 분리한다.
- 중복 코드를 발견하면 기존 코드를 재사용하거나 공통화한다.
- 단순한 기능에 불필요하게 복잡한 추상화나 패턴을 도입하지 않는다.
- 사용하지 않는 변수, import, 파일을 남기지 않는다.
- 접근성을 고려해 button, label, input 등 의미에 맞는 HTML 요소를 사용한다.
- 클릭 가능한 UI를 의미 없는 `div`로 구현하지 않는다.

## 4. 코드 스타일

- 들여쓰기는 2칸을 사용한다.
- 문자열은 작은따옴표를 사용한다.
- 세미콜론은 사용하지 않는다.
- 불필요한 trailing comma는 사용하지 않는다.
- 파일명과 컴포넌트명은 기존 프로젝트 규칙을 따른다.
- 스타일은 프로젝트에 설정된 SCSS 구조와 명명 규칙을 따른다.
- 인라인 스타일은 동적으로 계산해야 하는 값이 아니라면 사용하지 않는다.

## 5. 패키지 관리

- 새로운 패키지를 설치하기 전에 기존 패키지와 기본 API로 해결할 수 있는지 확인한다.
- 패키지가 필요하면 설치 이유와 용도를 먼저 설명한다.
- 사용자 승인 없이 상태관리, UI 프레임워크, CSS 프레임워크 등을 추가하지 않는다.
- 기존 패키지 버전을 임의로 변경하거나 전체 업데이트하지 않는다.
- `package-lock.json`의 불필요한 변경을 만들지 않는다.

## 6. 보안과 환경변수

- API 키, 토큰, 비밀번호 등의 비밀값을 코드에 직접 작성하지 않는다.
- 브라우저에 노출되는 `VITE_` 환경변수에 서버용 비밀키를 넣지 않는다.
- AI API, YouTube OAuth, 결제 API의 비밀키와 refresh token은 서버에서만 관리한다.
- 실제 비밀값이 들어가는 `.env.local`은 Git에 포함하지 않는다.
- 필요한 환경변수는 값 없이 `.env.example`에 변수명과 설명만 작성한다.
- 예제 데이터에 실제 개인정보나 계정 정보를 사용하지 않는다.

## 7. Git 작업 원칙

- 사용자 요청 없이 commit, push, pull, merge, rebase를 실행하지 않는다.
- 브랜치를 임의로 생성하거나 변경하지 않는다.
- 기존 변경사항을 reset, checkout, restore 등으로 삭제하지 않는다.
- 작업 전 `git status`를 확인하고 기존 변경사항을 보존한다.
- 요청받지 않은 파일을 삭제하지 않는다.
- 자동 생성 파일이나 `node_modules`, 환경변수 파일을 커밋 대상에 포함하지 않는다.

## 8. 작업 진행 방식

각 작업은 다음 순서로 진행한다.

1. 요청사항을 짧게 정리한다.
2. 관련 파일과 기존 구현을 확인한다.
3. 수정할 파일과 구현 방식을 설명한다.
4. 필요한 범위만 작업한다.
5. TypeScript 오류와 ESLint 오류를 확인한다.
6. 개발 서버 또는 빌드로 화면과 동작을 확인한다.
7. 수정한 파일과 검증 결과를 보고한다.

## 9. 검증 원칙

작업 완료 전 가능한 범위에서 다음 명령을 실행한다.

```bash
npm run lint
npm run build
```

- 오류가 발생하면 원인을 확인하고 해결한 뒤 완료했다고 보고한다.
- 테스트하지 못한 항목이 있다면 숨기지 말고 이유와 미검증 범위를 설명한다.
- 화면 작업은 레이아웃 깨짐, 텍스트 잘림, 오버플로, 버튼 크기, 반응형 상태를 확인한다.
- 콘솔 오류와 경고를 확인한다.
- 기존 정상 기능이 영향을 받지 않았는지 확인한다.

## 10. 작업 완료 보고 형식

작업을 마치면 다음 내용만 간결하게 보고한다.

- 구현하거나 수정한 내용
- 변경한 파일
- 실행한 검증 명령과 결과
- 남아 있는 문제 또는 다음 작업
- 사용자의 확인이 필요한 부분

"완료"라고 보고하기 전에 실제 파일 변경과 검증 결과를 확인한다.

## 11. Figma 참고 방법

- 이 프로젝트는 Figma Dev Mode MCP 서버(`figma-desktop`, `http://127.0.0.1:3845/mcp`)를 연결해서 사용한다.
- 작업할 화면/컴포넌트에 해당하는 Figma 프레임을 조회해 레이아웃·색상·간격·타이포 수치를 확인한 뒤 구현한다.
- MCP 서버가 연결되어 있지 않거나 조회에 실패하면, 임의로 수치를 추측하지 말고 사용자에게 Figma 데스크톱 앱에서 MCP 서버를 켜달라고 요청하거나 해당 화면의 캡처·수치를 요청한다.
- Figma 링크: `https://www.figma.com/design/s1vcVinETNj4YPpZH4z35B/AAY`
